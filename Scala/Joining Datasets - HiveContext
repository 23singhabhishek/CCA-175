
import org.apache.spark.sql.hive.HiveContext
val sqlc = new HiveContext(sc)
sqlc.sql("set spark.sql.shuffle.partitions = 10")

sc --> SparkContext

sqlc.sc("select * from orders limit 10").collect().foreach(println)
